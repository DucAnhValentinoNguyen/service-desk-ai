services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "${API_PORT:-8001}:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      DATABASE_PATH: /app/data/service-desk.db
      PLATFORM_DATA_PATH: ${PLATFORM_DATA_PATH:-/platform-data}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:3005,http://127.0.0.1:3005}
      MODEL_PROVIDER: ${MODEL_PROVIDER:-kimi}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_BASE_URL: ${OPENAI_BASE_URL:-https://api.openai.com/v1}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4o-mini}
      KIMI_API_KEY: ${KIMI_API_KEY:-}
      KIMI_BASE_URL: ${KIMI_BASE_URL:-https://api.moonshot.ai/v1}
      KIMI_MODEL: ${KIMI_MODEL:-kimi-k3}
      KIMI_REASONING_EFFORT: ${KIMI_REASONING_EFFORT:-low}
      LOCAL_BASE_URL: ${LOCAL_BASE_URL:-http://host.docker.internal:11434/v1}
      LOCAL_API_KEY: ${LOCAL_API_KEY:-ollama}
      LOCAL_MODEL: ${LOCAL_MODEL:-gemma4:26b}
      AI_TIMEOUT_SECONDS: ${AI_TIMEOUT_SECONDS:-20}
    volumes:
      - service_desk_data:/app/data
      - ${PLATFORM_HOST_DATA_PATH:-../ai-data-platform/data}:/platform-data:ro
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/healthz')"]
      interval: 10s
      timeout: 4s
      retries: 10
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "${FRONTEND_PORT:-3005}:3005"
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-/backend}
      SERVER_API_URL: ${SERVER_API_URL:-http://api:8000}
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped

volumes:
  service_desk_data:
